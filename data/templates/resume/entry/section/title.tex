{{- if .title -}}
{{- if .url -}}
	\textbf{\href{ {{- .url -}} }{ {{- .title -}} }}
	{{- if not .hiddenlink -}}
		{ \faIcon{link}}
	{{- end -}}
{{- else -}}
	\textbf{ {{- .title -}} }
{{- end -}}
{{- end -}}

{{- if .subtitle -}}
	{{- if .subtitle_url -}}
		\textit{ - \href{ {{- .subtitle_url -}} }{ {{ .subtitle -}} } }
	{{- else -}}
		\textit{ - {{ .subtitle -}} }
	{{- end -}}
{{- end -}}
{{- if .location -}}
	\textit{ ( {{- .location -}} ) }
{{- end -}}