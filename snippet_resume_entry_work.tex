\textcolor{accent}{
    \jblock{if entry.to is defined}
    \jvar{entry.from} --- \jvar{entry.to}
    \jblock{ else }
    \jvar{entry.from}
    \jblock{ endif }
} &
\jblock{if entry.url is defined}
\textbf{\href{\jvar{entry.url}}{\jvar{entry.role}}},
\jblock{ else }
\textbf{\jvar{entry.role}},
\jblock{ endif }
\textit{\jvar{entry.company}} \linebreak
\jvar{entry.description} \\\\ \hfill