\documentclass{article}

{{packages_resume}}

\urlstyle{same}

\definecolor{title}{rgb}{\jvar{theme.title}}
\definecolor{accent}{rgb}{\jvar{theme.accent}}
\definecolor{accentlight}{rgb}{\jvar{theme.accentlight}}

\newcolumntype{L}{>{\raggedright\let\newline\\\arraybackslash\hspace{0pt}}X}
\newcolumntype{C}[1]{>{\centering\let\newline\\\arraybackslash\hspace{0pt}}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\let\newline\\\arraybackslash\hspace{0pt}}p{#1}}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}

\begin{document}
\newgeometry{margin=2.25cm}

\setlength\LTpre{0pt}
\setlength{\LTpost}{0pt}

{{latex_personal}}

\textbf{\textcolor{title}{\uppercase{
			\jvar{education.school.label}
		}}} \jvar{theme.titlegap}\\  \hfill
{{resume_table_start}}
		{{resume_entry_education}}
{{resume_table_end}}

\jblock{if work.entries|count > 0}
\textbf{\textcolor{title}{\uppercase{\jvar{work.label}}}} \jvar{theme.titlegap}\\ \hfill
{{resume_table_start}}
	\jblock{for entry in work.entries}
		{{resume_entry_work}}
	\jblock{endfor}
{{resume_table_end}}
\jblock{ endif }

\jblock{if engineering.entries|count > 0}
\textbf{\textcolor{title}{\uppercase{\jvar{engineering.label}}}} \jvar{theme.titlegap}\\ \hfill
{{resume_table_start}}
	\jblock{for entry in engineering.entries}
	{{resume_entry_engineering}}
	\jblock{endfor}
{{resume_table_end}}
\jblock{ endif }

\jblock{if projects.newpage is defined}
\newpage
\jblock{ endif }
\textbf{\textcolor{title}{\uppercase{\jvar{projects.label}}}} \jvar{theme.titlegap}\\ \hfill
{{resume_table_start}}
	\jblock{for entry in projects.entries}
	\textcolor{accent}{
		\jblock{if entry.to is defined}
		\jvar{entry.from} --- \jvar{entry.to}
		\jblock{ else }
		\jvar{entry.from}
		\jblock{ endif }
	} &
	\jblock{if entry.url is defined}
	\textbf{\href{\jvar{entry.url}}{\jvar{entry.name}}},
	\jblock{ else }
	\textbf{\jvar{entry.name}},
	\jblock{ endif }
	\textit{\jvar{entry.location}} \linebreak
	\jvar{entry.description}                \\\\ \hfill
	\jblock{endfor}
{{resume_table_end}}

\jblock{if skills.newpage is defined}
\newpage
\jblock{ endif }
\textbf{\textcolor{title}{\uppercase{\jvar{skills.label}}}} \jvar{theme.titlegap}\\ \hfill
{{resume_table_start}}
	\jblock{for entry in skills.entries}
	\textcolor{accent}{\jvar{entry.category}} & \textit{\jvar{entry.entries|join(', ')}} \\ \hfill
	\jblock{endfor}
{{resume_table_end}}

\jblock{if leadership.entries|count > 0}
\textbf{\textcolor{title}{\uppercase{\jvar{leadership.label}}}} \jvar{theme.titlegap}\\ \hfill
{{resume_table_start}}
	\jblock{for entry in leadership.entries}
	\textcolor{accent}{
		\jblock{if entry.to is defined}
		\jvar{entry.from} --- \jvar{entry.to}
		\jblock{ else }
		\jvar{entry.from}
		\jblock{ endif }
	} &
	\textbf{\jvar{entry.name}} \linebreak
	\jvar{entry.description} \\\\ \hfill
	\jblock{endfor}
{{resume_table_end}}
\jblock{ endif }

\jblock{if activities.entries|count > 0}
\textbf{\textcolor{title}{\uppercase{\jvar{activities.label}}}} \jvar{theme.titlegap}\\ \hfill
{{resume_table_start}}
	\jblock{for entry in activities.entries}
	\textcolor{accent}{\jvar{entry.title}} & \jvar{entry.description} \\ \hfill
	\jblock{endfor}
{{resume_table_end}}
\jblock{ endif }

\end{document}
