Dear
\jblock{if recipient.name is defined}
    \jblock{if recipient.title is defined}
        \jvar{recipient.title}
    \jblock{ endif }
    \jvar{recipient.name}:
\jblock{ else }
    \jvar{recipient.position}:
\jblock{ endif }\\

\jblock{ if job.source == 'web' }
    When I saw your online listing for a \jvar{job.title} position,
\jblock{ elif job.source == 'generic' }
    When I saw your listing for a \jvar{job.title} position,
\jblock{ elif job.source == 'indeed' }
    When I saw your listing on Indeed for a \jvar{job.title} position,
\jblock{ elif job.source == 'custom' }
    \jvar{job.custom.source}\jblock{ endif }
\jblock{ if job.interest == 'custom' }
    \jvar{job.custom.interest}
\jblock{ elif job.source == 'generic' }
    I was immediately interested. I believe that I would be a good fit for this job.
\jblock{endif}